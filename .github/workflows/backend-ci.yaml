name: Backend-CI

on:
  push:
    branches:
      - cicd
    paths:
      - backend/**

permissions:
  contents: write

jobs:
  backend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # - name: Node Setup
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: 20

      # - name: Install Dependencies
      #   run: |
      #     cd backend
      #     npm install

      # - name: Snyk Scan
      #   uses: snyk/actions/node@master
      #   with:
      #     command: test
      #     args: --file=backend/package.json
      #   env:
      #     SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      # - name: Sonar Scan
      #   uses: SonarSource/sonarcloud-github-action@v2
      #   with:
      #     projectBaseDir: ./backend
      #   env:
      #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      # - name: Tagging
      #   id: version_tag
      #   if: github.ref == 'refs/heads/cicd'
      #   uses: mathieudutour/github-tag-action@v6.2
      #   with:
      #     tag_prefix: backend-v
      #     github_token: ${{ secrets.GITHUB_TOKEN}}
      #     release_branches: cicd

      # - name: Extract Version
      #   id: extract_version
      #   run: |
      #     TAG=${{ steps.version_tag.outputs.new_tag }}
      #     VERSION=${TAG#backend-v}
      #     echo "VERSION=$VERSION" >> $GITHUB_ENV

      # - name: Extract Branch Name
      #   run: |
      #     BRANCH=${GITHUB_REF#refs/heads/}
      #     echo "BRANCH=$BRANCH" >> $GITHUB_ENV

      # - name: Create Image Tag
      #   run: |
      #     IMAGE_TAG=${BRANCH}-${VERSION}
      #     echo "IMAGE_TAG=${IMAGE_TAG}" >> $GITHUB_ENV

      # - name: Docker Login
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}

      # - name: Setup Docker Buildx
      #   uses: docker/setup-buildx-action@v3

      # - name: Build Image
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: ./backend 
      #     load: true
      #     tags: |
      #       ${{ secrets.DOCKERHUB_USERNAME }}/book-store-backend:${{ env.IMAGE_TAG || github.sha }}
      #       ${{ secrets.DOCKERHUB_USERNAME }}/book-store-backend:latest
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max

      - name: Build Image
        run: |
          docker build --no-cache -t backend ./backend

      - name: Trivy Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: backend
          # image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/book-store-backend:${{ env.IMAGE_TAG || github.sha }}
          format: table
          exit-code: 1
          ignore-unfixed: true
          severity: CRITICAL

      # - name: Docker Push
      #   run: |
      #     docker push ${{ secrets.DOCKERHUB_USERNAME }}/book-store-backend:${{ env.IMAGE_TAG || github.sha }}
      #     docker push ${{ secrets.DOCKERHUB_USERNAME }}/book-store-backend:latest

      - name: Sucess Message
        run: echo "Sucessfully Done"